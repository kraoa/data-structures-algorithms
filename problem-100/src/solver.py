"""You'll implement this."""

import random
from typing import Optional
from reservoir import ListNode


class Solution:
    def __init__(self, head: Optional[ListNode]) -> None:
        self.head = head

    def get_random(self) -> int:
        return 0
