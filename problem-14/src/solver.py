"""You'll implement this."""


class MedianFinder:
    """
    Streaming median calculator.

    addNum(num)   — O(log n): add num to the internal data structure.
    findMedian()  — O(1): return the current median.

    Hint: maintain two heaps — a max-heap of the lower half and a min-heap
    of the upper half, kept balanced so their sizes differ by at most 1.
    Python's heapq is a min-heap; negate values to simulate a max-heap.
    """

    def __init__(self):
        pass

    def addNum(self, num: int) -> None:
        pass

    def findMedian(self) -> float:
        return 0.0
