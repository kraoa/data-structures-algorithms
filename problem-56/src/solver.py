"""You'll implement this."""

from typing import List


class Solver:
    """Searches a rotated sorted array in O(log n) time."""

    def search(self, nums: List[int], target: int) -> int:
        return -1
