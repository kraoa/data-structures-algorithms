"""You'll implement this."""

from typing import List


class Solver:
    """Counts elements to the right that are smaller than each element."""

    def count_smaller(self, nums: List[int]) -> List[int]:
        return []
