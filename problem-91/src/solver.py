"""You'll implement this."""

from typing import List, Optional

from view import TreeNode


class Solver:
    """Returns the right-side view of a binary tree."""

    def right_side_view(self, root: Optional[TreeNode]) -> List[int]:
        return []
