"""You'll implement this."""

from typing import List


class Solver:
    """Finds the minimum-cost path from top-left to bottom-right."""

    def min_path_sum(self, grid: List[List[int]]) -> int:
        return 0
