"""You'll implement this."""

from typing import List


class Solver:
    """Finds all combinations of candidates that sum to target."""

    def combination_sum(self, candidates: List[int], target: int) -> List[List[int]]:
        return []
